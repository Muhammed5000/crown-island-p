# Copy to /etc/crown-island/cron.env and edit; then: chmod 600 /etc/crown-island/cron.env
# Public origin the app is served on (no trailing slash).
APP_URL=https://your-domain.com
# MUST equal the CRON_SECRET in the app's environment (openssl rand -hex 32).
CRON_SECRET=replace-me
